package com.hyperwhisper.data.telemetry

enum class SessionType { CLOUD, ON_DEVICE }

enum class ColdStartKind { PROCESS_COLD, MODEL_COLD, WARM }

enum class NetworkType { WIFI, CELLULAR, ETHERNET, NONE, UNKNOWN }
