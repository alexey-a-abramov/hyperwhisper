<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- Foreground service for Whisper-model downloads. Without it the
         download process is reclaimed when the IME closes / app backgrounds. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".HyperWhisperApplication"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HyperWhisper"
        tools:targetApi="31">

        <!-- Input Method Service -->
        <service
            android:name=".service.VoiceInputMethodService"
            android:label="@string/ime_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <!-- Settings Activity -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:label="@string/settings_title"
            android:theme="@style/Theme.HyperWhisper"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- About Activity -->
        <activity
            android:name=".ui.about.AboutActivity"
            android:label="@string/about_title"
            android:theme="@style/Theme.HyperWhisper"
            android:exported="false" />

        <!-- Logs Activity -->
        <activity
            android:name=".ui.logs.LogsActivity"
            android:label="@string/logs_title"
            android:theme="@style/Theme.HyperWhisper"
            android:exported="false" />

        <!-- Latency Stats Activity -->
        <activity
            android:name=".ui.stats.StatsActivity"
            android:label="Latency Stats"
            android:theme="@style/Theme.HyperWhisper"
            android:parentActivityName=".ui.settings.SettingsActivity"
            android:exported="false" />

        <!-- Crash Report Activity -->
        <activity
            android:name=".utils.CrashActivity"
            android:label="App Crashed"
            android:theme="@style/Theme.HyperWhisper"
            android:exported="false"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:process=":crash" />

        <!-- Model-download foreground service. Started by WhisperModelDownloader
             on the first active download; stops itself when the queue empties. -->
        <service
            android:name=".data.ModelDownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- FileProvider for APK installation -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>

</manifest>
